import 'package:flutter/material.dart';

class ExercisesSection extends StatelessWidget {
  const ExercisesSection({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}